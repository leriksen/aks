apiVersion: v1
kind: Service
metadata:
  name: books-svc
  labels:
    app: books
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: books
